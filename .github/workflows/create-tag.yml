name: Call Tag on merge (uses reusable)

on:
  pull_request:
    types: [closed]
    branches: [main]

permissions:
  contents: write

jobs:
  call-tag:
    uses: juv-dev/workflows-frontend/.github/workflows/create-tag.yml@main
    secrets:
      GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}